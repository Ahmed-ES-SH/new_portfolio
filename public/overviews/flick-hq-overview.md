# PROJECT_OVERVIEW.md

> Auto-generated by `project-discoverer` · Low-cost shell scan · 2025-06-25

---

## 🗂 Project Identity

| Field | Value |
|---|---|
| **Project Name** | FLICK-HQ |
| **Type** | Full-Stack |
| **Primary Language** | TypeScript |
| **Runtime / Platform** | Node.js >= 20 |

---

## 🏗 Tech Stack

### Frontend

| Layer | Technology |
|---|---|
| Framework | Next.js 16 (App Router + Turbopack) |
| Styling | Tailwind CSS v4, shadcn/ui (new-york style) |
| State Management | Zustand, TanStack Query |
| Key Libraries | Framer Motion, Swiper, React Icons, Axios, Sonner |

### Backend

| Layer | Technology |
|---|---|
| Framework | NestJS v11 |
| Auth | JWT (passport-jwt), Google OAuth (passport-google-oauth20), Argon2 password hashing, httpOnly cookies, CSRF protection |
| ORM / DB Layer | TypeORM v0.3 |
| Queue / Jobs | Not detected |
| Key Libraries | Stripe, Pusher, @nestjs/swagger, @nestjs/cache-manager, @nestjs/throttler, @nestjs/schedule, @nestjs/event-emitter, Helmet, EJS mail templates |

### Database & Cache

| Role | Technology |
|---|---|
| Primary Database | PostgreSQL |
| Cache | cache-manager (configurable TTL) |
| Search | Not detected |

### Infrastructure & DevOps

| Aspect | Details |
|---|---|
| Containerized | No |
| Docker Services | N/A |
| CI/CD | Not detected |
| Package Manager | npm |

---

## 📁 Project Structure

```
./
├── backend/
│   ├── nest-cli.json
│   ├── package.json
│   └── src/
│       ├── auth/
│       ├── billing/
│       ├── common/
│       ├── config/
│       ├── contact/
│       ├── database/seeds/
│       ├── helpers/
│       ├── mail/
│       ├── modules/lists/
│       ├── notifications/
│       ├── plans-subscriptions/
│       └── user/
│       ├── db/migrations/
│       └── test/
├── frontend/
│   ├── .next/
│   ├── .opencode/
│   ├── next.config.ts
│   ├── package.json
│   └── app/   (Next.js App Router)
```

### Directory Roles

| Directory | Purpose |
|---|---|
| `backend/` | NestJS API server — authentication, billing, user management, content |
| `frontend/` | Next.js client application — entertainment discovery UI |
| `backend/src/auth/` | Authentication module (JWT, Google OAuth) |
| `backend/src/billing/` | Stripe billing integration |
| `backend/src/modules/lists/` | User lists feature |
| `backend/src/notifications/` | Notification system |
| `backend/src/mail/` | EJS email templates |
| `frontend/app/` | Next.js App Router pages |

---

## 🚀 Entry Points & Commands

### Scripts

**Backend:**

| Command | Purpose |
|---|---|
| `npm run start:dev` | Start NestJS dev server with watch mode |
| `npm run build` | Production build |
| `npm run start:prod` | Start production server |
| `npm run test` | Run Jest test suite |
| `npm run test:e2e` | Run end-to-end tests |
| `npm run migration:generate` | Generate TypeORM migration |
| `npm run migration:run` | Run pending migrations |
| `npm run seed:run` | Run database seed script |
| `npm run seed:sync-stripe` | Sync Stripe products |

**Frontend:**

| Command | Purpose |
|---|---|
| `npm run dev` | Start Next.js dev server with Turbopack |
| `npm run build` | Production build |
| `npm run start` | Start production server |
| `npm run test` | Run Vitest test suite |
| `npm run lint` | Run ESLint |

### Main Entry Files

| File | Role |
|---|---|
| `backend/src/main.ts` | NestJS application bootstrap |
| `frontend/app/` | Next.js App Router entry |

### Key Config Files

| File | Purpose |
|---|---|
| `backend/package.json` | Backend dependencies and scripts |
| `backend/nest-cli.json` | NestJS CLI configuration |
| `frontend/package.json` | Frontend dependencies and scripts |
| `frontend/next.config.ts` | Next.js framework configuration |
| `backend/.env.example` | Backend environment variable template |
| `frontend/.env.example` | Frontend environment variable template |

---

## 🔑 Environment Variables

> Keys only — never log values. Grouped by domain.

### Application
| Variable | Notes |
|---|---|
| `NODE_ENV` | Environment mode |
| `PORT` | Backend server port |
| `FRONTEND_URL` | Frontend URL for CORS/redirects |

### Database
| Variable | Notes |
|---|---|
| `DATABASE_URL` | PostgreSQL connection string |
| `DB_SSL_CERT` | Optional SSL certificate for DB |

### Auth
| Variable | Notes |
|---|---|
| `JWT_SECRET` | JWT signing secret |
| `JWT_EXPIRES_IN` | JWT expiration time |
| `AUTH_TOKEN` | Auth token |

### Stripe / Billing
| Variable | Notes |
|---|---|
| `STRIPE_SECRET_KEY` | Stripe API secret key |
| `STRIPE_RESTRICTED_KEY` | Stripe restricted key |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook signing secret |
| `STRIPE_API_VERSION` | Stripe API version |
| `STRIPE_SUCCESS_URL` | Checkout success redirect |
| `STRIPE_CANCEL_URL` | Checkout cancel redirect |
| `STRIPE_PORTAL_RETURN_URL` | Portal return redirect |
| `BILLING_ENABLED` | Enable/disable billing |
| `BILLING_DEFAULT_CURRENCY` | Default billing currency |

### Google OAuth
| Variable | Notes |
|---|---|
| `GOOGLE_CLIENT_ID` | Google OAuth client ID |
| `GOOGLE_CLIENT_SECRET` | Google OAuth client secret |
| `GOOGLE_CALLBACK_URL` | Google OAuth callback URL |

### Mail
| Variable | Notes |
|---|---|
| `MAIL_HOST` | SMTP host |
| `MAIL_PORT` | SMTP port |
| `MAIL_USER` | SMTP username |
| `MAIL_PASS` | SMTP password |
| `MAIL_FROM` | Sender email address |

### Pusher
| Variable | Notes |
|---|---|
| `PUSHER_APP_ID` | Pusher app ID |
| `PUSHER_KEY` | Pusher key |
| `PUSHER_SECRET` | Pusher secret |
| `PUSHER_CLUSTER` | Pusher cluster |

### Cache
| Variable | Notes |
|---|---|
| `CACHE_TTL` | Cache time-to-live |
| `CACHE_MAX_ITEMS` | Maximum cached items |

### Rate Limiting
| Variable | Notes |
|---|---|
| `THROTTLE_TTL` | Rate limit window (ms) |
| `THROTTLE_LIMIT` | Max requests per window |

### Other
| Variable | Notes |
|---|---|
| `SOCKET_CORS_ORIGIN` | Socket/CORS origin |
| `TMDB_API_KEY` | TMDB API key (backend) |
| `NEXT_PUBLIC_TMDB_API_KEY` | TMDB API key (frontend) |
| `NEXT_PUBLIC_BACKEND_URL` | Backend URL for frontend |
| `NEXT_PUBLIC_AUTH_TOKEN` | Auth token for frontend |

---

## 📦 Key Dependencies

### Production

**Backend:**
- `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express` — NestJS core framework
- `@nestjs/typeorm`, `typeorm`, `pg` — ORM + PostgreSQL driver
- `@nestjs/jwt`, `@nestjs/passport`, `passport-jwt` — JWT authentication
- `passport-google-oauth20` — Google OAuth
- `argon2` — Password hashing
- `stripe` — Payment processing
- `pusher` — Real-time events
- `@nestjs/cache-manager`, `cache-manager` — Caching
- `@nestjs/throttler` — Rate limiting
- `@nestjs/swagger` — API documentation
- `@nestjs/schedule` — Cron/scheduling
- `@nestjs/event-emitter` — Event system
- `helmet`, `csrf-csrf`, `cookie-parser` — Security
- `class-validator`, `class-transformer` — Request validation
- `serverless-http` — Serverless deployment

**Frontend:**
- `next` (v16), `react`, `react-dom` (v19) — Core framework
- `@tanstack/react-query` — Server state management
- `axios` — HTTP client
- `zustand` — Client state management
- `framer-motion` — Animations
- `swiper` — Carousel components
- `sonner` — Toast notifications
- `react-icons` — Icon library
- `sharp` — Image optimization

### Development

**Backend:**
- `@nestjs/cli`, `typescript`, `eslint`, `prettier`, `jest`, `ts-jest`, `supertest`, `ts-node`, `cross-env`

**Frontend:**
- `typescript`, `eslint`, `eslint-config-next`, `vitest`, `@vitest/coverage-v8`, `jsdom`, `tailwindcss`, `@tailwindcss/postcss`

---

## 🧩 Architecture Notes

### Detected Pattern
**API-First Separation** — Frontend and backend are separate apps communicating via REST/GraphQL. The backend is a standalone NestJS API with Swagger documentation, while the frontend is a standalone Next.js application that consumes the API via Axios and TanStack Query.

### Notable Conventions Detected
- **NestJS modular structure** — Feature modules organized by domain (auth, billing, user, lists, notifications)
- **Next.js App Router** — Modern routing with `app/` directory
- **TypeORM migrations** — Database schema management with seeds
- **Bilingual support** — English/Arabic RTL support in frontend
- **Cinema-grade UI** — Netflix-inspired dark theme with Framer Motion animations
- **Stripe integration** — Full billing workflow with webhooks and portal
- **Real-time features** — Pusher for live updates
- **Security hardening** — Helmet, CSRF, rate limiting, httpOnly cookies

---

## ⚠️ Discovery Gaps

- [ ] No Docker configuration found — deployment strategy unknown
- [ ] No CI/CD pipeline detected — build/deploy workflow unknown
- [ ] Database type inferred from `.env.example` — no docker-compose to confirm
- [ ] Queue/job system not detected — background processing strategy unknown

---

## 🔍 Discovery Metadata

| Field | Value |
|---|---|
| Scanned at | 2025-06-25 |
| Method | Low-cost shell scan (`ls`, `find`, `cat`, `head`, `grep`) |
| Files read by scout | 7 |
| Scout confidence | High |
| Orchestrator bash commands | 0 |
| Status | Generated |
