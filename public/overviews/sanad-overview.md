# PROJECT_OVERVIEW.md

> Auto-generated by `project-discoverer` · Low-cost shell scan · 2026-06-25T15:01:00Z

---

## 🗂 Project Identity

| Field | Value |
|---|---|
| **Project Name** | sanad |
| **Type** | Full-Stack |
| **Primary Language** | TypeScript |
| **Runtime / Platform** | Node.js (NestJS 11 backend, Next.js 16 frontend) |

---

## 🏗 Tech Stack

### Frontend

| Layer | Technology |
|---|---|
| Framework | Next.js 16 (App Router) |
| Styling | Tailwind CSS v4 + PostCSS |
| State Management | Zustand + TanStack React Query |
| Key Libraries | axios, framer-motion, stripe, socket.io-client, @tiptap/react, recharts, swiper, sonner, dompurify, react-icons |

### Backend

| Layer | Technology |
|---|---|
| Framework | NestJS 11 |
| Auth | JWT + Passport (JWT & Google OAuth20), RBAC, CSRF, Argon2 hashing |
| ORM / DB Layer | TypeORM 0.3.28 |
| Queue / Jobs | @nestjs/event-emitter (event-driven) |
| Real-time | Socket.io (platform-socket.io) |
| Key Libraries | stripe, @nestjs-modules/mailer, class-validator, class-transformer, helmet, cookie-parser, ejs, csrf-csrf |

### Database & Cache

| Role | Technology |
|---|---|
| Primary Database | PostgreSQL (via `pg` driver and TypeORM) |
| Cache | @nestjs/cache-manager (in-memory) |

### Infrastructure & DevOps

| Aspect | Details |
|---|---|
| Containerized | No — not detected |
| CI/CD | Not detected |
| Package Manager | pnpm (both apps use `pnpm-lock.yaml`; frontend has `package-lock.json` too) |
| Deployment | Vercel (backend vercel.json, frontend Next.js) |

---

## 📁 Project Structure

```
sanad/
├── backend/
│   ├── db/                  # Database migrations
│   ├── dist/                # Build output
│   ├── docs/                # Documentation
│   ├── integrations/        # Third-party integrations
│   ├── node_modules/
│   ├── src/                 # Application source
│   ├── test/                # Tests (e2e)
│   ├── .env.example
│   ├── nest-cli.json
│   ├── package.json
│   ├── tsconfig.json
│   └── vercel.json
├── frontend/
│   ├── app/                 # Next.js App Router source
│   ├── node_modules/
│   ├── public/              # Static assets
│   ├── .env.example
│   ├── next.config.ts
│   ├── postcss.config.mjs
│   ├── package.json
│   ├── pnpm-workspace.yaml
│   ├── proxy.ts             # Custom proxy configuration
│   └── tsconfig.json
├── .git/
└── .opencode/
```

### Directory Roles

| Directory | Purpose |
|---|---|
| `backend/src/` | NestJS application source — feature modules (auth, blog, cart, categories, contact, dashboard, notifications, payments, portfolio, services, service-orders, stripe, user, mail) |
| `backend/db/` | Database migration files |
| `backend/test/` | E2E test suite |
| `frontend/app/` | Next.js App Router — contains `[locale]/` for i18n, `app/api/` for API routes, `components/`, `hooks/`, `store/`, `helpers/`, `actions/`, `translations/`, `types/` |
| `frontend/public/` | Static assets |

---

## 🚀 Entry Points & Commands

### Scripts

| Command | Purpose |
|---|---|
| **Backend** | |
| `npm run start:dev` | Start NestJS dev server with hot-reload |
| `npm run build` | Production build |
| `npm run start:prod` | Start production server |
| `npm run test` | Run unit tests |
| `npm run test:e2e` | Run end-to-end tests |
| `npm run migration:run` | Run TypeORM migrations |
| `npm run seed` | Seed database |
| **Frontend** | |
| `npm run dev` | Start Next.js dev server |
| `npm run build` | Production build |
| `npm run start` | Start production server |
| `npm run lint` | Run ESLint |

### Main Entry Files

| File | Role |
|---|---|
| `backend/src/main.ts` | NestJS application bootstrap |
| `frontend/app/layout.tsx` | Next.js App Router root layout |

### Key Config Files

| File | Purpose |
|---|---|
| `backend/package.json` | Backend dependencies and scripts |
| `backend/nest-cli.json` | NestJS CLI configuration |
| `backend/tsconfig.json` | TypeScript configuration (ES2023, decorators) |
| `backend/.env.example` | Backend environment variable template |
| `backend/vercel.json` | Vercel deployment configuration |
| `frontend/package.json` | Frontend dependencies and scripts |
| `frontend/next.config.ts` | Next.js configuration |
| `frontend/tsconfig.json` | TypeScript configuration |
| `frontend/postcss.config.mjs` | PostCSS with Tailwind CSS v4 |
| `frontend/.env.example` | Frontend environment variable template |
| `frontend/pnpm-workspace.yaml` | PNPM workspace (monorepo-ready) |

---

## 🔑 Environment Variables

> Keys only — never log values. Grouped by domain.

### Application (Backend)
| Variable | Notes |
|---|---|
| `PORT` | Server port |

### Database (Backend)
| Variable | Notes |
|---|---|
| `DATABASE_URL` | PostgreSQL connection string |

### CORS (Backend)
| Variable | Notes |
|---|---|
| `FRONTEND_URL` | Allowed CORS origin |

### JWT (Backend)
| Variable | Notes |
|---|---|
| `JWT_SECRET` | Token signing secret |
| `JWT_EXPIRES_IN` | Token expiration duration |

### Google OAuth (Backend)
| Variable | Notes |
|---|---|
| `GOOGLE_CLIENT_ID` | Google OAuth client ID |
| `GOOGLE_CLIENT_SECRET` | Google OAuth client secret |
| `GOOGLE_CALLBACK_URL` | OAuth callback URL |

### Mail (Backend)
| Variable | Notes |
|---|---|
| `MAIL_HOST` | SMTP host |
| `MAIL_PORT` | SMTP port |
| `MAIL_USER` | SMTP username |
| `MAIL_PASS` | SMTP password |
| `MAIL_FROM` | Sender address |

### Stripe (Backend)
| Variable | Notes |
|---|---|
| `STRIPE_SECRET_KEY` | Stripe secret key |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook signing secret |

### Application (Frontend)
| Variable | Notes |
|---|---|
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Stripe publishable key (public) |
| `NEXT_PUBLIC_SITE_URL` | Public site URL |
| `NEXT_PUBLIC_API_BASE_URL` | Public API base URL |
| `API_BASE_URL` | Server-side API base URL |

---

## 📦 Key Dependencies

### Production
**Backend:**
- `@nestjs/common`, `@nestjs/core`, `@nestjs/config` — NestJS framework core
- `@nestjs/typeorm` + `typeorm` + `pg` — ORM with PostgreSQL
- `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `passport-google-oauth20` — Authentication
- `@nestjs/platform-socket.io` + `socket.io` — Real-time communication
- `@nestjs/swagger` — API documentation
- `@nestjs/throttler` — Rate limiting
- `stripe` — Payment processing
- `argon2` — Password hashing
- `class-validator`, `class-transformer` — Input validation
- `helmet` — Security headers
- `csrf-csrf` — CSRF protection
- `@nestjs-modules/mailer` — Email sending
- `@nestjs/event-emitter` — Event-driven architecture

**Frontend:**
- `next` (16.2.4) — React framework
- `react` (19.2.4), `react-dom` — UI library
- `@tanstack/react-query` — Server state management
- `axios` — HTTP client
- `zustand` — Client state management
- `framer-motion` — Animations
- `stripe` + `@stripe/react-stripe-js` + `@stripe/stripe-js` — Payment UI
- `socket.io-client` — Real-time client
- `@tiptap/react` + `@tiptap/starter-kit` — Rich text editor
- `recharts` — Charts
- `swiper` — Carousels
- `sonner` — Toast notifications
- `dompurify` — XSS sanitization
- `react-icons` — Icon library

### Development
**Backend:**
- `typescript`, `ts-jest`, `ts-node`, `ts-loader` — TypeScript tooling
- `jest`, `supertest` — Testing
- `eslint`, `prettier` — Code quality
- `@nestjs/cli`, `@nestjs/schematics`, `@nestjs/testing` — NestJS tooling

**Frontend:**
- `typescript` — TypeScript tooling
- `eslint`, `eslint-config-next` — Linting
- `@tailwindcss/postcss`, `tailwindcss` — Styling
- `@types/react`, `@types/react-dom`, `@types/node`, `@types/dompurify` — Type definitions

---

## 🧩 Architecture Notes

### Detected Pattern
- **API-First Separation** — Frontend (Next.js) and Backend (NestJS) are separate apps communicating via REST API. Backend exposes a Swagger-documented API consumed by the frontend.

### Backend Module Layout (Feature-based)
The `backend/src/` directory is organized by domain module:
- `auth/` — Authentication (JWT, Google OAuth, RBAC guards)
- `blog/` — Blog/CMS module
- `cart/` — Shopping cart management
- `categories/` — Product/service categories
- `contact/` — Contact form handling
- `dashboard/` — Admin dashboard
- `notifications/` — Event-driven notifications
- `payments/` — Payment processing
- `portfolio/` — Portfolio showcase
- `services/` + `service-orders/` — Services booking
- `stripe/` — Stripe integration
- `user/` — User management
- `mail/` — Email templates and mailing

### Frontend App Structure
The `frontend/app/` directory follows Next.js App Router conventions:
- `[locale]/` — Internationalization (localized routes)
- `api/` — API route handlers (BFF layer)
- `actions/` — Server actions
- `components/` — Feature-organized UI components (auth, cart, dashboard, payment, userdashboard, website, global)
- `helpers/` — Business logic helpers (auth, cart, contact, dashboard, payments)
- `hooks/` — Custom React hooks (articleGrid, notifications, services)
- `store/` — Zustand state stores
- `lib/` — Library code (payments)
- `translations/` — i18n translation files
- `types/` — TypeScript type definitions
- `constants/` — Application constants

### Notable Conventions Detected
- **App Router pattern detected** — `frontend/app/` directory with `layout.tsx`, `[locale]/` for i18n
- **Feature-based module structure** in both backend (`src/<module>/`) and frontend (`app/components/`, `app/helpers/`)
- **TypeORM migrations** managed via `typeorm` CLI commands
- **pnpm workspace** configured (`pnpm-workspace.yaml`) enabling potential monorepo workflows
- **API proxy** configured via `frontend/proxy.ts`

---

## 🔍 Discovery Metadata

| Field | Value |
|---|---|
| Scanned at | 2026-06-25T15:01:00Z |
| Method | Low-cost shell scan (`ls`, `find`, `cat`, `head`, `grep`) |
| Files read by scout | 11 |
| Scout confidence | High |
| Orchestrator bash commands | 3 |
| Status | Generated |
